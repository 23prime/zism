# See: https://lefthook.dev/configuration/

pre-push:
  jobs:
    - name: mise check
      run: mise check

pre-commit:
  jobs:
    - name: mise check
      run: mise check

    - name: gitleaks protect
      run: mise gitleaks-protect
